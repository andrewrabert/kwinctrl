[package]
name = "kwinctrl"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
tempfile = "3"
rand = "0.8"
zbus = { version = "5", features = ["blocking"] }
regex = "1"

[profile.release]
lto = true
strip = true
